# Backend (Node + Express + Tesseract.js)

## Setup
1. `cd backend`
2. `npm install`
3. `npm start`

The server runs on port 5000 by default. POST `multipart/form-data` to `/api/extract` with field name `image`.
